# Patch Integration Summary — wasla-version-0.1

This release is a consolidated build of all patch versions:
- v1 → v11 (included)

Validation:
- The file set of v11 is a superset of all previous patch archives (no missing files).

Key additions in v0.1:
- Unified i18n (Arabic/English) + RTL/LTR
- Marketing-grade landing page + refreshed UI polish
- Admin QA Dashboard + actions (seed/index/KPI)
- Visual Search UI and API (FAISS + filters + optional CLIP)
- Registration & Store Wizard fully persisted in DB
